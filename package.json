{
  "name": "www.hamsternik.com",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "preview": "npx @11ty/eleventy --serve"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-rss": "^2.0.4",
    "prettier": "^3.2.5"
  }
}
